import express from 'express';
const router = express.Router();

// Import and connect routes
// import connectAccountRoutes from './routes/accountRoutes';

// Connect each route
// connectAccountRoutes(router);

export default router;