export enum UserRole {
    COORDINATOR = 'coordinator',
    WORKER = 'worker'
}

export enum StaffRole {
    MANAGER = 'manager',
    CHEF = 'chef',
    SOUS_CHEF = 'sous_chef',
    SERVER = 'server',
    BARTENDER = 'bartender',
    BARBACK = 'barback',
    DISHWASHER = 'dishwasher',
    BUSSER = 'busser',
    RUNNER = 'runner',
    HOST = 'host',
    ARRANGER = 'arranger',
    CLEANER = 'cleaner',
}